import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Hefesto — Catálogo mitológico de hardware',
  description: 'Componentes, especificações e listagens para montar PCs.',
}

export default function SearchPage() {}
